<div class="card has-table has-mobile-sort-spaced">
  <header class="card-header">
    <div class="card-header-title">
      <span class="icon"><i class="mdi mdi-account-multiple"></i></span>
      <h1><%= t('app.room_index.title') %></h1>
    </div>
    <a href="#" class="card-header-icon">
      <span class="icon"><i class="mdi mdi-reload"></i></span>
    </a>
  </header>

  <div class="card-content">
    <div id="rooms">
      <% @rooms.each do |room| %>
        <div class="b-table has-pagination">
          <div class="table-wrapper has-mobile-cards">
            <table class="table is-fullwidth is-striped is-hoverable is-sortable is-fullwidth">
              <thead>
                <tr>
                  <th></th>
                  <th><%= t('app.room.name') %></th>
                  <th><%= t('app.room.description') %></th>
                  <th><%= t('app.room.maximum_capacity') %></th>
                  <th><%= t('app.room.status') %></th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td class="is-image-cell">
                    <div class="image">
                      <img src="https://avatars.dicebear.com/v2/initials/rebecca-bauch.svg" class="is-rounded">
                    </div>
                  </td>
                  <td data-label="Name"><%= room.name %></td>
                  <td data-label="Company"><%= room.description %></td>
                  <td data-label="City"><%= room.maximum_capacity %></td>
                  <td data-label="City"><%= room.status %></td>
                  <td class="is-actions-cell">
                    <div class="buttons is-right">
                      <button class="button is-small is-primary" type="button">
                        <%= link_to t('app.room_index.show'), room %>
                        <span class="icon"><i class="mdi mdi-eye"></i></span>
                      </button>
                      <button class="button is-small is-primary" type="button">
                        <%= link_to t('app.room_show.edit'), edit_room_path(room) %>
                        <span class="icon"><i class="mdi mdi-pencil"></i></span>
                      </button>
                      <div type="button" data-target="sample-modal">
                        <%= button_to t('app.room_show.delete'), room, method: :delete,
                                      form: { data: { turbo_confirm: t('app.controller.notice_delete') } },
                                      class: "button is-small is-danger jb-modal" %>
                      </div>
                    </div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
